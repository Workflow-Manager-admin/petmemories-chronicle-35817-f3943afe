// Proxy entry added so usage of 'react-router-dom' is available if direct import fails for some dev environments.
// Remove if not needed and install the actual 'react-router-dom' npm package instead.
export * from "react-router-dom";
